{% extends "base.twig" %}

{% block content %}
    <h3>FRONT</h3>
    {% if latest_posts_by_category %}
        <ul>
            {% for category_slug, post in latest_posts_by_category %}
                <li>
                    {# <article class="tease tease-{{ post.type }}" id="tease-{{ post.ID }}"> #}
                        {# <h2 class="h2"><a href="{{ post.link }}">{{ post.title }}</a></h2> #}
                      {% include 'parts/teaser.twig' %}
                    {# </article> #}
                </li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
