<!DOCTYPE html>
<html {{ site.language_attributes }}>

<head>
    <meta charset="{{ site.charset }}">
   {{ function('wp_head') }}
</head>

<body class="{{ body_class }}">
   
</body>
{{ function('wp_footer') }}
</html>

